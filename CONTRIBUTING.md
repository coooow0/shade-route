# 기여 안내

이 문서는 향후 공개 전환을 위한 초안입니다. 현재 외부 기여 접수를 확정한다는 뜻은 아닙니다.

## 개발 환경

Node.js와 npm을 준비한 뒤 저장소 루트에서 실행합니다.

```bash
npm ci
npm run dev
```

변경 사항은 다음 명령으로 검증합니다.

```bash
npm run test:coverage
npm run typecheck
npm run lint
npm run build
```

## 변경 원칙

- 위치 정보, 검색어, 키, 토큰, 개인 로컬 경로를 코드·로그·문서·테스트 데이터에 넣지 않습니다.
- 클라이언트 번들에 포함되는 `VITE_*` 환경 변수에는 공개 가능한 값만 둡니다. 앱인토스 배포 키 같은 비밀은 로컬 CLI 환경이나 CI secret으로만 주입하고, `.env.example`에는 값이 아닌 변수 이름과 설명만 기록합니다.
- 경로 안전 정책이나 데이터 스키마를 바꾸면 경계값 테스트와 관련 보안 문서를 함께 갱신합니다.
- 생성된 서울 데이터를 바꾸면 원본 URL, 내려받은 시각, SHA-256, 라이선스와 생성 명령을 기록합니다.
- 앱인토스 `.ait`, `dist`, `coverage`, `node_modules`는 커밋하지 않습니다.
- 보안 문제는 공개 이슈 대신 [SECURITY.md](./SECURITY.md)의 절차로 제보합니다.

## 변경 제안

하나의 변경에는 하나의 목적만 담고, 사용자에게 보이는 동작과 검증 결과를 설명해 주세요. 커밋 메시지는 `<type>: <description>` 형식을 사용합니다.
