# 그늘길 프로젝트 컨텍스트

> 다른 채팅이나 개발 도구에서 이 프로젝트를 이어서 작업할 때 먼저 읽는 문서다. 현재 구현과 계획을 구분하고, 이미 합의한 제품 범위를 유지한다.

## 한눈에 보기

그늘길은 출발 시각의 건물 그림자를 계산해 햇빛을 덜 받는 서울 도보 경로를 찾는 앱인토스 미니앱이다. 사용자는 출발지와 도착지를 검색하고 `빠른길`, `균형`, `그늘우선` 세 경로의 소요 시간, 거리, 햇빛 노출 시간, 예상 그늘 비율을 비교한다.

- 한국어 앱 이름: `그늘길`
- appName: `shade-route`
- 형태: 앱인토스 WebView 미니앱
- 주요 사용자: 여름철 서울에서 3km 이내를 걷는 사람
- 핵심 가치: 빠른길보다 얼마나 더 걸리고 햇빛을 얼마나 덜 받는지 알려준다.
- 현재 범위: 서울 전역, 경로 길이 3km 이내
- 프로젝트 위치: 저장소 루트

## 해결하려는 문제

일반 지도는 가장 빠른 길을 잘 찾지만, 한여름 보행자가 받을 햇빛은 고려하지 않는다. 같은 목적지라도 건물 배치와 출발 시각에 따라 그늘이 생기는 길이 달라진다. 그늘길은 시간과 햇빛 노출 사이의 차이를 수치로 보여주고 사용자가 상황에 맞는 경로를 고르게 한다.

이 앱의 핵심 문장은 다음과 같다.

> 그늘길은 빠른길보다 2분 더 걸리지만 햇빛을 8분 덜 받아요.

정확한 숫자는 경로 계산 결과에서 만든다. 지도 기술 자체보다 이 차이를 사용자가 즉시 이해하게 만드는 것이 중요하다.

## 사용자 흐름

1. 서울 현재 기온, 체감온도, 자외선을 확인한다.
2. 출발지와 도착지를 장소명 또는 주소로 검색한다.
3. 필요하면 현재 위치를 출발지로 설정한다.
4. `지금`, `30분 뒤`, `1시간 뒤` 중 출발 시각을 선택한다.
5. `그늘 경로 찾기`를 누른다.
6. 빠른길, 균형, 그늘우선 경로를 비교한다.
7. 선택한 경로를 지도에서 확인하고, 필요하면 `지도에 내 위치 표시`를 누른다.
8. 상세 도보 안내는 필요할 때만 펼쳐본다.

## 현재 구현된 기능

### 장소와 위치

- 서울 OpenStreetMap 원본에서 미리 만든 약 5만 건의 로컬 장소·주소 인덱스를 검색한다.
- 지하철역, 카페, 음식점, 병원·약국, 회사, 학교·도서관, 공원, 건물명, 도로명 주소를 지원한다.
- 장소 검색어를 외부 검색 API로 보내지 않는다.
- 앱인토스 위치 권한으로 현재 위치를 가져오며, 일반 브라우저에서는 브라우저 위치 API를 대체 수단으로 쓴다.
- 현재 위치가 서울 밖이거나 정확도가 낮으면 사용자에게 원인을 안내한다.
- 결과 지도에서 사용자가 요청하면 포그라운드 실시간 위치, 오차 범위, 경로 근처 이동 여부를 표시한다.
- 정확한 위치가 경로 밖에서 3번 연속 확인될 때만 경로 이탈 가능성을 알린다. GPS 좌표를 경로 위로 강제 보정하지 않는다.

### 경로와 그림자

- 건물 높이와 태양의 고도·방향으로 출발 시각의 예상 그림자를 만든다.
- 보행 그래프에서 A* 탐색으로 세 가지 경로를 계산한다.
- `빠른길`: 거리와 시간을 우선한다.
- `균형`: 시간과 그늘을 함께 고려한다.
- `그늘우선`: 햇빛 노출을 더 강하게 피한다.
- 각 경로에 소요 시간, 거리, 햇빛 노출 시간, 예상 그늘 비율을 표시한다.
- 지도 선을 예상 그늘, 햇빛, 접근 구간으로 구분한다.
- 회전, 직진, 계단 등을 포함한 단계별 도보 안내를 만든다.

### 날씨와 추천

- Open-Meteo에서 서울 현재 기온, 체감온도, 자외선, 낮·밤 여부를 가져온다.
- 현재 날씨와 자외선에 따라 빠른길, 균형, 그늘우선 중 하나를 추천한다.
- 날씨 요청에는 사용자 위치가 아닌 서울시청 인근 대표 좌표를 보낸다.
- 30분·1시간 뒤 경로의 건물 그림자는 선택한 시각으로 계산한다.
- 30분·1시간 뒤 날씨 예보는 아직 추천에 반영하지 않는다. 이때는 `미래 날씨 미반영`을 표시하고 균형 경로를 추천한다.
- 날씨 요청이 실패해도 장소 검색과 경로 계산은 계속 사용할 수 있다.

### 지도와 결과

- 검색과 결과를 두 화면 상태로 나눈다. 경로를 찾은 뒤에는 지도를 뷰포트에 가득 채우고 검색 폼과 날씨를 숨긴다.
- 결과 상단 얇은 바에 출발지 → 도착지 요약과 검색으로 돌아가는 버튼을 둔다.
- 세 경로는 하단 바텀 시트의 지표 표(시간·거리·햇빛·예상 그늘)로 한눈에 비교한다. 컬럼 헤더가 모드 선택 탭 역할을 한다.
- Leaflet과 OpenStreetMap 타일로 실제 지도 배경을 표시한다.
- 출발지와 도착지 마커, 경로 범례, OSM 저작자 표시를 제공한다.
- 실시간 현재 위치는 파란 점과 정확도 원으로 표시하고, `지도 자동 이동`을 켜거나 끈다. 관련 버튼은 시트에 가리지 않도록 지도 우측 상단에 둔다.
- 상세 도보 안내는 시트 안에 기본으로 펼쳐 두고, 사용자가 원할 때 접을 수 있다.
- 지도 타일이나 지도 초기화가 실패해도 경로 요약과 상세 안내는 유지한다.

## 제품 범위와 결정

### 유지할 결정

- 출품 전 서비스 범위는 서울 전역과 3km 이내 도보다.
- 지도에서 점을 찍는 방식보다 장소·주소 검색을 기본 입력으로 사용한다.
- 빠른길 하나만 주지 않고 세 경로의 시간과 햇빛 차이를 비교한다.
- 사용자 위치는 저장하지 않는다.
- 전국 확장, 자동 재탐색, 음성 턴바이턴 안내, 백그라운드 위치 추적, 로그인, 결제, 리워드는 현재 범위에서 제외한다.
- 광고·결제가 없는 챌린지 프로토타입을 먼저 완성한다.

### 제품 판단

- 여름·무더위와 직접 연결되어 2026년 7월 앱인토스 바이브코딩 챌린지에 적합하다.
- 필요할 때 바로 여는 도구이므로 현재 단계에서는 독립 앱보다 토스 미니앱이 잘 맞는다.
- 장기 성공 가능성은 기능 수보다 경로 신뢰도, 검색 편의, 실제 그늘과 예상치의 차이를 얼마나 잘 관리하는지에 달려 있다.
- 전국이나 웹으로 확장하기 전에 서울에서 검색부터 추천 경로 확인까지 한 번에 성공하는 경험을 검증한다.

## UX 방향

별도 라우팅 시스템 없이 앱 상태를 검색과 결과 두 단계로 나눈다.

### 검색 상태

- 날씨를 작은 카드로 보여준다.
- 출발지, 도착지, 출발 시각을 선택한다.
- `그늘 경로 찾기`를 주 행동으로 둔다.

### 결과 상태

- 상단 얇은 바에 `출발지 → 도착지` 요약과 `← 검색으로 돌아가기` 버튼을 둔다.
- 지도가 남은 뷰포트를 가득 채운다. 지도의 실시간 위치 토글은 시트에 가리지 않도록 우측 상단에 둔다.
- 하단 바텀 시트에 빠른길·균형·그늘우선을 4행 지표 표(시간·거리·햇빛·예상 그늘)로 나란히 비교한다. 컬럼 헤더가 모드 선택 탭을 겸한다.
- 시트 안에 출발 시각 칩과 상세 도보 안내를 함께 둔다. 상세 도보는 기본으로 펼쳐 두고 사용자가 원하면 접을 수 있다.
- 결과 화면 스크롤은 시트 내부의 상세 안내와 주석·한계 표시를 위해 남아 있다.

현재 코드에 이 흐름을 구현했다.

## 다음 우선순위

### P0 — 출품 전

1. 320px·390px iPhone 폭과 앱인토스 샌드박스에서 검색→결과→현재 위치 표시를 확인한다.
2. 앱 아이콘 URL을 `granite.config.ts`에 반영한다.
3. 앱 대표 이미지, 한 줄 소개, 챌린지 주제 연관성 문구를 준비한다.
4. 2026년 7월 29일까지 최신 `.ait`를 콘솔에 등록하고 출품 폼을 제출한다.

### P1 — 재방문

1. 최근 경로를 기기 내부에 저장하고 현재 시각 기준으로 다시 계산한다.
2. 추천 경로를 기본 선택하되 세 경로를 쉽게 비교하게 한다.
3. 개인정보 없이 경로 검색 시작·성공·실패와 모드 선택 퍼널을 기록한다.

### P2 — 유입

1. `그늘 68% · 햇빛 7분 절약` 형태의 결과 공유를 검토한다.
2. 앱인토스 공유 링크와 딥링크를 현재 WebView SDK에 맞게 연동한다.

## 정확도와 알려진 한계

- 계산 결과는 예상치다. 실제 그늘은 현장 공사, 나무, 간판, 건물 형태, OSM 데이터 시점에 따라 달라질 수 있다.
- 높이가 없는 건물은 기본 높이 12m로 계산한다.
- 현재는 건물 그늘을 중심으로 계산하며 가로수 그늘은 포함하지 않는다.
- 경로 안내는 OSM 보행 그래프에서 만든 것으로 전문 내비게이션의 실시간 안내를 대체하지 않는다.
- 목적지가 보행 그래프에서 멀거나 서울 밖이면 경로를 만들지 못할 수 있다.
- `서울 전역`은 서울 전체의 전처리 데이터를 포함한다는 뜻이다. 모든 서울 좌표에서 검색과 경로 생성을 보장하지 않는다.
- 3km를 넘는 경로는 지원하지 않는다.
- Vite 프로덕션 main JS 번들은 약 1.46MB, gzip 약 463KB이고, 경로 계산은 약 69KB의 별도 module Worker 청크로 분리된다. main 청크의 500kB 권장 크기 경고는 남아 있다.

사용자 화면에는 실제 현장과 다를 수 있다는 안내를 유지한다. 정확도를 과장하는 표현은 쓰지 않는다.

## 개인정보와 외부 서비스

- 장소 검색: 로컬 인덱스. 검색어를 외부로 전송하지 않는다.
- 현재 위치: 경로와 지도 표시에만 사용하고 저장하지 않는다.
- 실시간 위치 구독은 결과 화면을 떠나거나 `위치 숨기기`를 누를 때, 또는 앱·문서가 백그라운드로 전환될 때 종료한다. 다시 돌아오면 사용자가 명시적으로 시작해야 한다.
- 지도: 표시 중인 주변 영역이 OpenStreetMap 타일 서버에 전달될 수 있다.
- 날씨: 사용자 위치 대신 고정된 서울 대표 좌표를 Open-Meteo에 보낸다.
- Open-Meteo 데이터는 CC BY 4.0이며 앱 화면에 출처와 가공 사실을 표시한다.
- Open-Meteo 무료 API는 비상업 프로토타입에서만 사용한다. 광고·결제 또는 상용 출시 전에는 유료 customer API와 서버 프록시로 전환한다.
- 정확도 피드백: 결과 화면 하단 위젯이 사용자가 명시적으로 `보내기`를 누른 경우에만 만족도·경로 지표·선택 메모·요청 도시를 `VITE_FEEDBACK_WEBHOOK_URL` 웹훅으로 보낸다. 좌표·검색어·계정 정보는 보내지 않고, 같은 경로는 `localStorage`에 저장해 두 번 요청하지 않는다. 웹훅 URL이 비어 있으면 위젯 자체를 렌더링하지 않는다. 세부 세팅은 [`docs/feedback-webhook.md`](./docs/feedback-webhook.md)에 정리한다.

## 기술 구조

- React 18 + TypeScript + Vite
- `@apps-in-toss/web-framework` 2.x
- `@toss/tds-mobile`과 `@toss/tds-mobile-ait`
- Leaflet: 지도 표시
- SunCalc: 태양 위치 계산
- RBush: 그림자 공간 인덱스
- Vitest + Testing Library: 단위·통합 테스트
- 별도 백엔드 없음

주요 디렉터리:

- `src/App.tsx`: 검색, 위치, 시간 선택, 결과 화면의 전체 상태
- `src/components/`: 장소 검색, 날씨, 지도, 상세 경로 UI
- `src/domain/routing/`: 그래프, 그림자, A*, 경로 요약
- `src/domain/places/`: 서울 장소 인덱스 로딩과 검색
- `src/domain/location/`: 현재 위치와 서비스 영역 검증
- `src/domain/weather/`: 현재 날씨 로딩과 추천 규칙
- `scripts/`: 서울 OSM 데이터 전처리
- `public/data/`: 전처리한 서울 보행로·건물 데이터

### WebView 자원 보호

- compact schema 2 루팅 타일은 요소·좌표 수를 객체 확장 전에 검증한다. legacy schema 1 루트 타일은 받지 않는다.
- 그래프 노드·간선·차수·연결 요소, 그늘 후보, 경로 세그먼트, 상세 길안내에 명시적 상한을 둔다.
- Leaflet 경로는 casing·connector·shade·sunny multi-polyline 최대 4개로 묶고, 상세 길안내 DOM은 사용자가 펼칠 때만 만든다.
- 상한은 2026-07 서울 데이터 812개 타일의 실측값을 기준으로 한다. 데이터 갱신 때 실측값, 상한, 경계값 테스트를 함께 재검토한다.
- 세부 근거와 남은 위험은 [`docs/security/pr1-webview-resource-hardening.md`](./docs/security/pr1-webview-resource-hardening.md)에 기록한다.

### OSM 보행 안전 정책

- 생성기와 앱 런타임은 `src/domain/routing/walkability.mjs`의 보행 판정을 함께 사용한다.
- 구체적인 `foot=*`를 일반 `access=*`보다 우선하며, 공개 보행은 `yes`, `designated`, `permissive`만 허용한다.
- conditional 접근, 폐기 way, 고난도 산길, 방향 불명 에스컬레이터는 시간·상태 평가기가 없으므로 제외한다.
- manifest schema 3가 제외 way, 차단 노드, fallback way, 보행 방향, 아티팩트 해시와 provenance를 필수로 전달한다. 구 manifest는 fail-closed로 거부한다.
- 차단 노드에 닿는 세그먼트는 그래프에서 만들지 않고, A*는 명시적인 보행·에스컬레이터 방향을 강제한다.
- fallback 도로는 스냅, 컴포넌트 선택, 모든 경로 모드에서 같은 1.5배 비용을 사용한다. 보행 공간 없이 `sidewalk=separate`만 있는 간선도로는 제외한다.
- 2026-07-15 재생성 결과는 타일 812개, unique way 115,236개, 제외 way 2,073개, 차단 노드 114개, fallback way 11,157개, 방향 way 7개다.
- 출발지·목적지와 스냅 지점을 잇는 최대 100m·150m connector는 아직 제한 geometry와의 교차를 검사하지 않는다. endpoint 접근 등급과 공개 출입구 모델을 후속 보안 작업으로 남긴다.
- 가장 가까운 edge가 명시적 보행 방향과 반대면 같은 연결 요소의 다음 스냅 후보를 탐색하지 않아 경로가 안전하게 실패할 수 있다. 검색 성공률 개선 때 방향 인식 다중 스냅을 검토한다.
- 세부 근거와 남은 위험은 [`docs/security/pr2-osm-pedestrian-semantics.md`](./docs/security/pr2-osm-pedestrian-semantics.md)에 기록한다.

### 데이터 무결성과 경로 Worker

- 앱 번들의 generated integrity root가 manifest와 장소 인덱스 SHA-256을 직접 고정하고, manifest가 812개 타일·장소·public 경계 아티팩트의 정확한 바이트와 release ID를 고정한다.
- 로더는 manifest, 선택한 개별 타일, 장소 인덱스를 JSON 파싱 전에 Web Crypto SHA-256으로 검증한다. 변조·부분 배포·스테일 캐시가 섞이면 경로를 만들지 않고 fail-closed로 종료한다.
- `npm run data:seoul`은 원본 PBF 출처 메타데이터, 파일 크기, SHA-256과 런타임 서울 경계를 같은 실행에서 생성한다. `npm run data:verify`는 공유 manifest schema로 파일 집합·크기·해시·release ID를 전수 검증하고 원본 경계와 런타임 TypeScript의 drift도 거부하며, `npm run build`의 prebuild에서 자동 실행된다.
- 경로 데이터 fetch·decode·그래프·그늘·A* 작업은 요청당 module Web Worker에서 실행한다. 30초 제한, 재검색 무효화, unmount, `pagehide`, 문서 hidden에서 Worker를 종료한다.
- Worker 입력·출력은 별도 schema·수량 검증을 거친다. 허용한 도메인 오류 코드만 UI로 전달하고 예외 상세는 숨긴다.
- 앱인토스 iOS/Android 실기기의 module Worker·Web Crypto 호환성은 샌드박스에서 확인해야 한다. 미지원 환경에서 무결성 검증을 생략하는 fallback은 두지 않았다.
- 세부 근거와 남은 위험은 [`docs/security/pr3-artifact-integrity-worker.md`](./docs/security/pr3-artifact-integrity-worker.md)에 기록한다.

## 실행과 검증

프로젝트 루트에서 실행한다.

```bash
npm run dev
```

전체 검증:

```bash
npm run data:verify
npm run test:coverage
npm run typecheck
npm run lint
npm run build
```

서울 데이터를 다시 만들 때 원본 PBF가 `/private/tmp/south-korea-latest.osm.pbf`에 있어야 한다.

```bash
npm run data:seoul
```

2026-07-15 서울 데이터는 `/private/tmp/south-korea-latest.osm.pbf`를 사용했다. 파일 크기는 282,641,335 bytes, 수정 시각은 2026-07-11 19:00:42 +0900, SHA-256은 `828c27e48293061e4187b068052f711c9926837f3b85c4245e95e052f35c4e77`이다. 다운로드 URL과 실제 다운로드 시각은 확인할 수 없어 다음 데이터 갱신부터 함께 기록한다.

이 디렉터리는 `main` 브랜치를 기본으로 쓰는 Git 저장소이며, GitHub 저장소 `coooow0/shade-route`를 `origin`으로 연결한다. 빌드 산출물, 커버리지, `node_modules`, 앱인토스 `.ait` 파일은 커밋하지 않는다. 공개 전환 전에는 [`docs/public-release-checklist.md`](./docs/public-release-checklist.md)를 확인한다.

2026년 7월 15일 PR 3 마지막 로컬 검증 결과:

- 37개 파일, 테스트 229개 통과
- 문장 커버리지 87.01%, 분기 커버리지 83.38%, 함수 커버리지 92.28%, 라인 커버리지 90.78%
- manifest, places, public·runtime boundary, 서울 타일 812개 아티팩트 무결성·drift 전수 검증 통과
- 타입 검사와 ESLint 통과
- 앱인토스 `.ait` 빌드 통과
- `shade-route.ait` 23,125,319 bytes
- main minified JavaScript 1,456.65KB, gzip 462.95KB와 별도 route Worker 69.42KB를 생성했다. main 청크의 500kB 권장 크기 초과 경고는 남아 있다.

2026년 7월 13일 실기기 화면 검증 결과:

- 390px 결과 화면에서 상단 요약·가로 경로 카드·지도가 한 화면 흐름으로 보이는 것을 확인했다.
- 320px 화면에서 버튼과 카드가 가로로 넘치지 않는 것을 확인했다.

### 알려진 개발도구 의존성 보안 경고

2026년 7월 15일 `npm audit --omit=dev`에서 31건(치명 1, 높음 12, 보통 2, 낮음 16)이 보고됐다. 핵심 경로는 `@apps-in-toss/web-framework@2.10.5` 하위의 Granite·Fastify·React Native CLI 및 빌드 도구 체인이며, `@fastify/middie`, `fastify`, `find-my-way`, `fast-uri`, `@babel/core`, `esbuild`, `fast-xml-parser`, `ip`가 포함된다.

- 정적 `dist`와 `.ait` 산출물에는 Fastify, Middie, find-my-way, fast-uri가 포함되지 않는다.
- 개발 서버는 `localhost`에만 열며 외부 네트워크에 공개하지 않는다.
- 공식 최신 `@apps-in-toss/web-framework@2.10.6`도 같은 Granite 1.0.20 의존성을 고정하므로 현재 안전한 상위 업데이트가 없다.
- 일부 전이 의존성은 `npm audit fix`를 제안하지만, 자동 수정·`--force`·Fastify 계열의 임의 major override는 앱인토스 빌드 호환성을 전체 검증하는 별도 의존성 PR 없이 적용하지 않는다.
- 앱인토스/Granite가 수정 버전을 배포하면 공식 호환 버전으로 갱신하고 전체 테스트와 `.ait` 빌드를 다시 검증한다.

## 챌린지와 출시

- 챌린지: [2026년 7월 바이브코딩 챌린지](https://toss.im/apps-in-toss/blog/2607_vibecoding_challenge)
- 출품 기간: 2026년 7월 1일~29일
- 첫 번들 등록 마감: 2026년 7월 29일
- 1차 심사: 2026년 8월 1일~26일 사용 지표
- 최종 심사: 테마 적합성과 사용자 경험 등 완성도 종합 평가

콘솔 등록은 완료했다. 최신 번들 업로드와 출품 폼 제출은 남아 있다.

## 작업할 때 지킬 것

- 새 기능보다 검색 성공률, 결과 이해도, iPhone 사용성을 먼저 개선한다.
- 현재 구현과 계획을 섞어 설명하지 않는다.
- 서울·3km 제한을 임의로 넓히지 않는다.
- 정확도와 개인정보 관련 안내를 제거하지 않는다.
- API 키나 인증 정보를 코드와 문서에 기록하지 않는다.
- 기능을 변경하면 테스트, 이 문서, README의 설명을 함께 갱신한다.
